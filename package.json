{
  "name": "ebr-bpm-pwa",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema PWA de Evaluacion Basada en Riesgo y BPM",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "validate": "npm run build -w @ebr-bpm/risk-engine && npm run build -w @ebr-bpm/report-engine && npm run validate --workspaces --if-present",
    "test": "npm run validate"
  },
  "engines": {"node": "^20.19.0 || >=22.12.0"}
}
