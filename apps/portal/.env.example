# El navegador usa el mismo origen del portal. En desarrollo Vite proxifica /v1 y /health.
VITE_API_PROXY_TARGET=http://localhost:3000
# Clave Ed25519 pública SPKI DER, codificada en base64; debe corresponder a la clave privada de Core.
VITE_OFFLINE_PERMIT_PUBLIC_KEY_BASE64=
