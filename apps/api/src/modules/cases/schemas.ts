import{z}from'zod';
export const id=z.string().uuid();export const page=z.coerce.number().int().positive().default(1);export const limit=z.coerce.number().int().min(1).max(100).default(20);const date=z.string().regex(/^\d{4}-\d{2}-\d{2}$/);
export const listSchema=z.object({page,limit,origin:z.enum(['INSTITUTIONAL_PROGRAM','HEALTH_ALERT','COMPLAINT']).optional(),status:z.enum(['PENDING_REVIEW','PENDING_ASSIGNMENT','ASSIGNED','NO_ACTION','REFERRED','CLOSED']).optional(),priority:z.enum(['LOW','MEDIUM','HIGH','URGENT']).optional(),companyId:id.optional(),establishmentId:id.optional(),dateFrom:date.optional(),dateTo:date.optional(),search:z.string().trim().min(1).max(100).optional()}).strict().refine(x=>!x.dateFrom||!x.dateTo||x.dateTo>=x.dateFrom);
export type ListInput=z.infer<typeof listSchema>;
